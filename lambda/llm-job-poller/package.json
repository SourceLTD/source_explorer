{
  "name": "llm-job-poller",
  "version": "1.0.0",
  "description": "AWS Lambda function to poll OpenAI for LLM job status updates",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r function.zip dist node_modules",
    "deploy": "npm run package && aws lambda update-function-code --function-name llm-job-poller --zip-file fileb://function.zip",
    "deploy:complete": "./deploy-complete.sh",
    "destroy": "./destroy.sh",
    "test": "npm run build && tsx test-local.ts"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.932.0",
    "@prisma/client": "^5.22.0",
    "openai": "^4.73.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.138",
    "@types/node": "^20.11.0",
    "pg": "^8.16.3",
    "prisma": "^5.22.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
